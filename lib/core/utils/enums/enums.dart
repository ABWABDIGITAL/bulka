enum UserRole { user, company, visitor, unkown }

enum VerifyAccountMethod { email, phone, unkown }

// ignore: constant_identifier_names
enum ProductCardWidgetDisplay {
  posts,
  jobs,
  properties,
  cars,
  general,
  // ignore: constant_identifier_names
  import_and_export
}

enum AdMediaType { image }

// ignore: constant_identifier_names
enum CategoryTypes { posts, jobs, properties, cars, general, import_and_export }

// ignore: constant_identifier_names
enum SliderTypes { ex_ternal, internal }

enum FieldType { text }
