/* import 'package:flutter/material.dart';

class ChosenSkillsList extends StatelessWidget {
  const ChosenSkillsList({super.key});

  @override
  Widget build(BuildContext context) {
    return List.generate();
  }
} */