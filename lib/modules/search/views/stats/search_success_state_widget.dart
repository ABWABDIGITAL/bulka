import 'package:flutter/material.dart';

class SearchSuccessStateWidget extends StatelessWidget {
  const SearchSuccessStateWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
